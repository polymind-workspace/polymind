{
  "name": "polymind",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "./dev.sh",
    "dev:web": "pnpm --filter web dev",
    "dev:admin": "pnpm --filter admin dev",
    "dev:api": "cd apps/api && uv run alembic upgrade head && uv run uvicorn app.main:app --reload --port 8300",
    "build": "pnpm --filter web build && pnpm --filter admin build",
    "build:web": "pnpm --filter web build",
    "build:admin": "pnpm --filter admin build",
    "typecheck": "pnpm --filter web typecheck && pnpm --filter admin tsc",
    "typecheck:web": "pnpm --filter web typecheck",
    "typecheck:admin": "pnpm --filter admin tsc",
    "test": "pnpm --filter web test",
    "preview": "pnpm --filter web preview"
  },
  "devDependencies": {
    "pnpm": "^10.0.0"
  },
  "packageManager": "pnpm@10.0.0"
}
